This folder contains tiny sample files for manual smoke tests.

Suggested quick checks:
- Copy `sample-set` somewhere outside the repo.
- Add one or two duplicate copies of `alpha.txt`.
- Run the tool in dry-run mode first.
- Inspect the generated `planned_deletions.csv` before trying `--apply`.
